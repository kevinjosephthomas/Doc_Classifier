name: CI/CD Pipeline - Full Stack

on:
  push:
    branches:
      - main

jobs:

  # =========================
  # 🧪 BACKEND TESTS
  # =========================
  backend-tests:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install Backend Dependencies
      run: |
        pip install -r requirements.txt
        pip install -r backend/requirements.txt

    - name: Run Backend Unit Tests
      run: pytest tests/ -v


  # =========================
  # 🎨 FRONTEND BUILD
  # =========================
  frontend-build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'

    - name: Install Frontend Dependencies
      run: |
        cd frontend
        npm install

    - name: Build Frontend
      run: |
        cd frontend
        npm run build


  # =========================
  # 🚀 BUILD + SECURITY + PUSH
  # =========================
  build-and-push:
    needs: [backend-tests, frontend-build]
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    # 🔐 Install Trivy
    - name: Install Trivy
      run: |
        wget https://github.com/aquasecurity/trivy/releases/latest/download/trivy_0.70.0_Linux-64bit.deb
        sudo dpkg -i trivy_0.70.0_Linux-64bit.deb

    # ⚡ Cache Trivy DB
    - name: Cache Trivy DB
      uses: actions/cache@v3
      with:
        path: ~/.cache/trivy
        key: trivy-cache

    # 🔍 Scan source code (dependencies + secrets)
    - name: Scan Filesystem
      run: |
        trivy fs --exit-code 1 --severity CRITICAL .

    # 🐳 Docker login
    - name: Login to DockerHub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    # =========================
    # 🐳 BACKEND IMAGE
    # =========================
    - name: Build Backend Image
      run: |
        docker build -t ${{ secrets.DOCKER_USERNAME }}/doc-classifier-backend:latest ./backend

    - name: Debug Docker Images
      run: docker images

    - name: Scan Backend Image
      run: |
        trivy image --exit-code 1 --severity CRITICAL \
        ${{ secrets.DOCKER_USERNAME }}/doc-classifier-backend:latest

    - name: Push Backend Image
      run: |
        docker push ${{ secrets.DOCKER_USERNAME }}/doc-classifier-backend:latest

    # =========================
    # 🐳 FRONTEND IMAGE
    # =========================
    - name: Build Frontend Image
      run: |
        docker build -t ${{ secrets.DOCKER_USERNAME }}/doc-classifier-frontend:latest ./frontend

    - name: Scan Frontend Image
      run: |
        trivy image --exit-code 1 --severity CRITICAL \
        ${{ secrets.DOCKER_USERNAME }}/doc-classifier-frontend:latest

    - name: Push Frontend Image
      run: |
        docker push ${{ secrets.DOCKER_USERNAME }}/doc-classifier-frontend:latest

    # =========================
    # ☸️ K8s SECURITY SCAN
    # =========================
    - name: Scan Kubernetes Manifests
      run: |
        trivy config --exit-code 1 --severity CRITICAL k8s/