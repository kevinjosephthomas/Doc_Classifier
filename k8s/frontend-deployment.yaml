apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: doc-classifier-frontend
  template:
    metadata:
      labels:
        app: doc-classifier-frontend
    spec:
      containers:
      - name: frontend
        image: kevinjosephthomas/doc-classifier-frontend:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 80
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "250m"
            memory: "256Mi"
